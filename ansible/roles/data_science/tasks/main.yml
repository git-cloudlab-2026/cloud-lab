---
- name: Install data science system packages
  ansible.builtin.apt:
    name:
      - python3-pip
      - python3-venv
    state: present

- name: Install Python data science packages
  ansible.builtin.pip:
    name:
      - jupyter
      - pandas
      - numpy
    executable: pip3
    extra_args: --break-system-packages
